pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            name = 'Huskuraft'
            url = 'https://repo.huskuraft.dev/releases'
        }
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven {
            name = 'Huskuraft'
            url = 'https://repo.huskuraft.dev/releases'
        }
    }
}

rootProject.name = 'effortless'

